## 제출 방법

각자 자신의 이름으로 브랜치를 파서, 그 브랜치에 자신의 백엔드 구현을 커밋합니다.

```bash
git checkout main
git pull origin main
git checkout -b backend-<본인이름>   # 예: backend-seonung

mkdir backend-<본인이름>
cd backend-<본인이름>
# 구현 진행...

git add backend-<본인이름>
git commit -m "feat: <언어/프레임워크> 백엔드 1주차 구현"
git push origin backend-<본인이름>
```

- **브랜치 이름 = 폴더 이름**을 동일하게 맞춥니다. (`backend-<본인이름>`)
- 매주 마감일까지 자신의 브랜치에 push하고, PR을 열어서 제출합니다.
  - PR 제목: `feat: <이름> - <주차>주차 제출` (예: `feat: seonung - 1주차 제출`)
  - base는 `main`, compare는 `backend-<본인이름>`으로 엽니다.
- PR은 **머지하지 않고 열어만 둡니다.** (리뷰/기록용으로 남겨두고, 실제 머지는
  전체 완료 후 한 번에 정리하거나 스터디장 판단에 따름)
- 매주 같은 브랜치에 이어서 커밋하면 됩니다. 주차마다 브랜치를 새로 팔 필요는
  없습니다.

## 커밋 컨벤션

커밋 메시지는 아래 형식을 따릅니다.

```
<타입>: <변경 내용 요약>

예시)
feat: Todo 생성/조회 API 구현
fix: 날짜 응답이 로컬 타임존으로 나가던 버그 수정
docs: 백엔드 실행 방법 README에 추가
refactor: 인증 미들웨어 구조 정리
test: 로그인 API 유닛 테스트 추가
chore: 의존성 버전 업데이트
```

**타입 목록**

| 타입 | 의미 |
|---|---|
| `feat` | 새 기능 추가 |
| `fix` | 버그 수정 |
| `docs` | 문서만 수정 (README, 주석 등) |
| `refactor` | 동작 변화 없는 코드 구조 개선 |
| `test` | 테스트 코드 추가/수정 |
| `chore` | 빌드 설정, 의존성, 잡일성 변경 |
| `perf` | 성능 개선 |

- 요약은 한 줄로, 마침표 없이, 명령형으로 씁니다. (`~했습니다` ❌ → `~구현` ⭕)
- 커밋 하나에 기능 하나. "이것저것 수정" 같은 뭉뚱그린 커밋은 지양합니다.

## 브랜치 전략

```
main                        ← 항상 정상 동작하는 상태만 유지
└── backend-<본인이름>       ← 각자 작업 브랜치 (예: backend-seonung)
```

- 각자 자신의 `backend-<이름>` 브랜치에서만 작업합니다.
- 다른 사람의 `backend-<이름>` 폴더는 건드리지 않습니다.

## 기타 주의할 점

- **다른 사람의 `backend-<이름>` 폴더는 절대 건드리지 않습니다.** 참고하고
  싶으면 읽기만 하고, 코드를 복사해오는 것도 지양합니다 (각자 스스로
  구현해보는 게 목적입니다).
- **PR 올리기 전 반드시 로컬에서 실제로 실행해서 확인합니다.**
- **`frontend/`와 실제로 연결해서 화면까지 확인 후 제출합니다.** API가
  명세대로 응답하는 것과, 프론트에서 실제로 잘 동작하는 것은 다른
  문제입니다.
- **막혔다고 명세를 임의로 바꿔서 구현하지 않습니다.** (`openapi.yaml`은
  변경 금지 원칙, 위 "명세 준수 규칙" 참고)
- **1주차를 못 끝냈어도 다음 주차로 넘어가지 않습니다.** 순서대로 쌓이는
  구조라, 인증(2주차) 없이는 이후 주차 기능 대부분이 의미가 없어집니다.
- **커밋 전 시크릿 유출 여부를 한 번 더 확인합니다.** `.env` 파일이나
  DB 접속 정보가 실수로 커밋에 포함되지 않았는지 `git status`, `git diff`로
  확인 후 push합니다.
- **본인 백엔드 폴더 안의 코드/설정은 자유입니다.** DB 종류, ORM 사용 여부,
  폴더 구조 등은 명세(`openapi.yaml`)만 지킨다면 전부 개인 선택입니다.
